# localQQTransferProxy

## Configuration

1. Copy `.env.example` to `.env`.
2. Fill in your QQ and Claude credentials in `.env`.
3. Never commit `.env`.

Required runtime settings are validated at startup.
